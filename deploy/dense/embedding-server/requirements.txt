fastapi>=0.100
uvicorn[standard]>=0.23
onnxruntime>=1.17
tokenizers>=0.15
numpy>=1.24
pydantic>=2.5
einops>=0.7
# PyTorch runtime for models with custom architectures (e.g. nomic_bert / CodeRankEmbed)
# that optimum cannot natively export to ONNX. CPU-only wheel is installed in Dockerfile.
sentence-transformers>=2.7
